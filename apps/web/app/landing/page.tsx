import type { Metadata } from "next";
import Link from "next/link";
import { MODELS, VARIATIONS, type ModelKey } from "../_lib/variations";

export const metadata: Metadata = {
  title: "Landing page variations | Heavenly Travel",
  description:
    "Six landing page concepts for Heavenly Travel, generated by Claude Opus 5 and Claude Fable 5.1.",
};

const GROUPS: ModelKey[] = ["opus", "fable"];

export default function LandingIndex() {
  return (
    <div className="min-h-screen bg-neutral-50 font-sans text-neutral-900">
      <main className="mx-auto max-w-3xl px-5 pt-16 pb-32">
        <Link
          href="/"
          className="text-sm text-neutral-500 underline-offset-4 hover:underline"
        >
          Home
        </Link>
        <h1 className="mt-4 text-3xl font-semibold tracking-tight sm:text-4xl">
          Landing page variations
        </h1>

        <nav aria-label="Landing page variations" className="mt-10 space-y-10">
          {GROUPS.map((model) => (
            <section key={model} aria-labelledby={`group-${model}`}>
              <h2
                id={`group-${model}`}
                className="text-sm font-medium text-neutral-500"
              >
                {MODELS[model].name}
              </h2>
              <ul className="mt-3 divide-y divide-neutral-200 border-y border-neutral-200">
                {VARIATIONS.filter((v) => v.model === model).map((v) => (
                  <li key={v.href}>
                    <Link
                      href={v.href}
                      className="flex items-baseline justify-between gap-4 py-4 hover:bg-neutral-100 focus-visible:outline-2 focus-visible:outline-offset-2"
                    >
                      <span className="font-medium">Option {v.option}</span>
                      <span className="text-sm text-neutral-500">
                        {v.seed}
                      </span>
                    </Link>
                  </li>
                ))}
              </ul>
            </section>
          ))}
        </nav>
      </main>
    </div>
  );
}
